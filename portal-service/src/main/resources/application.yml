feign:
  hystrix:
    enabled: false

# ribbon的超时时间
ribbon:
  ReadTimeout: 30000
  ConnectTimeout: 30000

# zuul
zuul:
  routes:
    user:
      path: /api-user/**
      service-id: user-service

portal:
  version: 1